// ============================================
// Co-Helm — Main
// ============================================

document.addEventListener("DOMContentLoaded", () => {
  CoHelmSidebar.init();
  CoHelmChat.init();
  CoHelmFullscreen.init();
});