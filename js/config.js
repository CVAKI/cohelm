// ============================================
// Co-Helm — Config
// ============================================

const CO_HELM_CONFIG = {
  // Your Cloudflare Worker proxy URL
  WORKER_URL: "https://co-helm-proxy.sivanand2004.workers.dev",
};
